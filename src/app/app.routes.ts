import { Routes } from '@angular/router';
import { TPDirectivesComponent } from './tp-directives/tp-directives.component';

export const routes: Routes = [
    { path: '', component: TPDirectivesComponent },
];
